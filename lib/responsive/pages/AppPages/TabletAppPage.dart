import 'package:flutter/material.dart';

class TabletAppPage extends StatelessWidget {
  const TabletAppPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Logged in Tablet app page"),
    );
  }
}
