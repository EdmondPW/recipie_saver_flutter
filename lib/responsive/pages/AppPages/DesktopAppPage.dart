import 'package:flutter/material.dart';

class DesktopAppPage extends StatelessWidget {
  const DesktopAppPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Logged in Desktop app page"),
    );
  }
}
